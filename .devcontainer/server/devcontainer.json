{
  "name": "Server",
  "dockerComposeFile": ["../../docker-compose.yml", "../../dev.yml"],
  "service": "app",
  "workspaceFolder": "/app",
  "runArgs": [
    "--env-file",
    "../../.env.dev"
  ],
  "runServices": [
    "app",
    "db",
    "redis"
  ]
}
